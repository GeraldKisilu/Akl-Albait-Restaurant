# Task: Extract Menu Data into menuData.ts

## Steps
- [x] Step 0: Plan approved by user
- [x] Step 1: Create `src/app/menuData.ts` with image URLs and base data
- [x] Step 2: Edit `App.tsx` — Add import of menuItems + imageMap helper
- [x] Step 3: Edit `App.tsx` — Remove `IMG` object block (replaced with `imageMap` lookup)
- [x] Step 4: Edit `App.tsx` — Replace hardcoded `CATEGORIES` with derived version from menuData
- [x] Step 5: Edit `App.tsx` — Update `ITEMS` array to use imageMap instead of IMG.xxx
- [x] Step 6: `DEAL_ITEMS` and `MEALS_DEALS` — unchanged (use their own inline images)
- [x] Step 7: Build verified — 0 errors, production build successful 🟢
